# Software Audit Toolkit

## Gems

To get list of gems, latest version, app version, and more, run:

`ruby ../software-audit-toolkit/gems.rb` from within the application directory.

Requires single quoted Gemfile and gem bundle to be up to date.

Uses RubyGems API to fetch info. Requires manual intervention for repositories that are not hosted on Rubygems.
